esphome:
  name: test-mouse-jiggler-arduino

esp32:
  board: esp32dev
  framework:
    type: arduino

external_components:
  - source:
      type: local
      path: ../../components

ble_mouse_jiggler:
